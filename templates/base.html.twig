<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Isabela Fartusnic{% endblock %}</title>

    {# Descriere pentru motoarele de căutare #}
    <meta name="description" content="O platformă modernă pentru managementul cazurilor clinice și procedurilor medicale.">

    {# Favicons și iconițe pentru diverse platforme #}
    <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon-16x16.png') }}">
    <link rel="manifest" href="{{ asset('site.webmanifest') }}">
    <link rel="mask-icon" href="{{ asset('safari-pinned-tab.svg') }}" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">

    {# --- STILURI --- #}
    {# Încărcarea stilurilor compilate de Webpack Encore #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {# Exemplu de încărcare a fonturilor externe - se încarcă înaintea CSS-ului principal #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">

</head>
<body>
{#
Containerul principal pentru aplicația React.
React va prelua controlul acestui div și va randa toate componentele în interiorul lui.
#}
<div id="root">

</div>

{# --- SCRIPTURI --- #}
{#
Este esențial ca scripturile să fie încărcate la finalul <body>-ului
pentru a permite DOM-ului să se încarce complet înainte de execuția JavaScript-ului.
#}
{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>
